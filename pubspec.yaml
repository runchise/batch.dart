name: batch
description: A lightweight and powerful batch library written in Dart. You can easily create a CLI batch program in Dart with this library.
version: 0.2.0
homepage: https://github.com/myConsciousness/batch.dart

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  collection: ^1.15.0
  cron: ^0.4.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  import_sorter: ^4.6.0
